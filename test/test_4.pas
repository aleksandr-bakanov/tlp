program hello;

var i:integer;
    j:real;
const str = 'TODO';
const int = 123;
label lab, bel;

operator + (a, b : string) asd:real;
begin

end;

function func : string;
        var i: char;
            r: real;
        begin
            {new (r);}
        end;

begin
    write((1 + 2));
    str := func() + 1;
    read(i);
    j := 3 * (i + 78.4 / 12);
    while (i <> 12) do
    begin
        {Make it yourself}
        while (i <> 12) do
        begin
            {Make it yourself}
            str := concat('sad', str);
            write (str);
        end;
        if (length(c) = 4) then
            goto lab;
    end;
    ds := concat('sad', str);
lab:write('END');
end.