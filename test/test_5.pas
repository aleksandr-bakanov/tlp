program hello;

var i:integer;
    j:integer;
    a:integer;
    b:integer;
    c:real;
    d:boolean;
    e:boolean;
    s:string;
    s1:string;

const str = 'TODO';
const int = 123;
label lab, bel;

operator + (a: integer; b : string) asd:real;
begin

end;

function func : string;
var i: boolean;
    r: real;
begin
    read(t);
end;

begin
    { write ( (i + 78.4 / 12) * 3, 'sdf', 234, 12.34, str, b, c, d );
    i := a + b;
    i := 'efwee';
    i := 234;
    i := 12.34;
    i := b;
    i := str; }
    { b := 1;
    a := 23 + b;
    write(a);
    i := length(str);
    write(i);
    b := a + i * 100;
    write('This is trollolo', b); }
    { d := true;
    if (d ) then
        begin
            i := d + 9 * 2 - 2;
            write (183);
        end
    else
        begin
            a := length('l,kmjnhbgvf');
            write (a);
        end; }
    d := true;
    a := 0;
    b := 5;
    i := 1;
    j := 1;
    goto qwe;
    while (a < b and d) do
    begin
        write(23423);
        a := a + 1;
    end;
    qwe:write('olololololol');
    { str := concat('qwe', 'rty'); }
    write(str);
    s := 'asd';
    write(s);
    s1 := s;
    write(s1);
end.






